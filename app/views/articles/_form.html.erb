<%= bootstrap_form_for(@article , layout: :horizontal) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

        <%= f.text_field :subject %>

        <%= f.file_field :photo %>

        <%= f.collection_select :category_id , Category.all , :id , :subject , {:class => ''} %>

        <div class="form-group">
            <label class="control-label col-sm-2"><%=f.label :content%></label>

            <div class="col-sm-10">
                <%= f.cktext_area :content %>
            </div>
        </div>

        <div class="form-group">
          <label class="control-label col-sm-2"><%=f.label :preview%></label>

          <div class="col-sm-10">
            <%= f.cktext_area :preview %>
          </div>
        </div>



        <%= f.submit %>

<% end %>
